// main.c
#include "bookManage.h"
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
  int width = 0;
  int height = 0;
  printf("size of width: ");
  scanf("%d", &width);
  printf("size of height: ");
  scanf("%d", height);

  int (*arr)[width] =

      return 0;
}
